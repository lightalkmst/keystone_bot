const assert = require ('assert')

const {
  reset_globals,
  basic_mock_request,
  test_checks,
} = require ('../_test_utils')

const status = require ('../../src/handlers/status')

describe ('status', () => {
  beforeEach (reset_globals)

  it ('reports the number of players that have joined the next tournament')

  it ('reports the status of each player in the tournament')
})
