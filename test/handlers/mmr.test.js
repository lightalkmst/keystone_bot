const assert = require ('assert')

const {
  reset_globals,
  basic_mock_request,
} = require ('../_test_utils')

const mmr = require ('../../src/handlers/mmr')

describe ('mmr', () => {
  beforeEach (reset_globals)

  it ('updates a player\'s mmr')
})
